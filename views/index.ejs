<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"/>
</head>
<body>
    <div class="container">
        <h3>Nhà xuất bản ABC</h3>
    <h1 class="text-center">Danh mục các bài báo</h1>
    <button onclick="window.location='/addForm'">Add new Article </button>
    <form contenteditable="true" id="delete" method="POST" action="/delete"  enctype="multipart/form-data">
        <table class="table table-striped table-bordered">
            <thead>
                <th scope="col">STT</th>
                <th scope="col">ID</th>
                <th scope="col">Tên bài báo</th>
                <th scope="col">Tên nhóm tác giả</th>
                <th scope="col">Số trang</th>
                <th scope="col">Năm xuất bản</th>
                <th scope="col">Trang bìa</th>
                <th>Function</th>
            </thead>
            <tbody>
                <% for(let i=0; i < baiBaos.length; i++) { %>
                    <tr>
                        
                        <td><%=i%></td>
                        <td><%=baiBaos[i].id%></td>
                        <td><%=baiBaos[i].Name%></td>
                        <td><%=baiBaos[i].Author%></td>
                        <td><%=baiBaos[i].Pages%></td>
                        <td><%=baiBaos[i].ReleasedYear%></td>
                        <td><%=baiBaos[i].Image%></td>
                        <td><button id="btnDelete" name="<%=baiBaos[i].id%>" value="<%=baiBaos[i].id%>">DELETE</button></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </form>
    </div>
</body>
</html>